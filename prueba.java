public class prueba {

    i=0;
    while (i++ <= 1000)
    aleatorio=random(0,1)
    delay(1s)
    if (aleatorio <= 1/60)
        tempo.start()
        agregarPaciente()
        tempo.stop()
        t.agregarPaciente=tempo.getElapsedTime()
        print.t.agregarPaciente
    if(aleatorio <= 1/120)
    
}
